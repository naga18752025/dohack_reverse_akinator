let realPrompt = null;

const express = require("express");
const cors = require("cors");
require("dotenv").config();
const fetch = require("node-fetch"); // node-fetch@2 を使ってください！

const app = express();

const corsOptions = {
  origin: "https://naga18752025.github.io", // 自分の GitHub Pages だけ許可
  methods: ["POST"],
};
app.use(cors(corsOptions));

app.use(express.json());

app.post("/api/openai", async (req, res) => {
  try {
    const { prompt, info1, info2, info3, Q, temperature } = req.body;
    console.log(temperature);

    if(prompt === 0){
      const themeLog = info1;
      realPrompt = `
      次のカテゴリのうちの1つを、**無作為に選んで**ください。
      そのカテゴリに該当する、日本人の多くが知っている「名詞」を1つだけ思い浮かべてください。（例示しているものである必要はありません）

      【カテゴリ】
      - 歴史上の人物（例：「織田信長」「坂本龍馬」「紫式部」など）
      - 有名人・キャラクター（例：「ドラえもん」「ピカチュウ」「イチロー」など）
      - 職業（例：「先生」「医者」「警察官」「大工」など）
      - 日用品（例：「椅子」「傘」「スマートフォン」「財布」など）
      - 家電・機械（例：「テレビ」「冷蔵庫」「パソコン」「掃除機」など）
      - 玩具・遊具（例：「ブランコ」「カード」「ゲーム機」「人形」など）
      - 動物（例：「犬」「ゾウ」「カンガルー」「イルカ」など）
      - 鳥（例：「スズメ」「カラス」「フクロウ」など）
      - 魚・海の生き物（例：「サメ」「タコ」「金魚」「イルカ」など）
      - 昆虫（例：「カブトムシ」「チョウ」「トンボ」など）
      - 植物（例：「桜」「ひまわり」「竹」「松」など）
      - 食べ物（例：「寿司」「ラーメン」「カレー」「おにぎり」など）
      - 果物（例：「みかん」「りんご」「バナナ」「スイカ」など）
      - 飲み物（例：「お茶」「コーヒー」「ジュース」「コーラ」など）
      - 国・地域（例：「日本」「アメリカ」「フランス」など）
      - 都市・地名（例：「東京」「大阪」「京都」「ニューヨーク」など）
      - 施設（例：「学校」「図書館」「病院」「遊園地」など）
      - 自然の場所（例：「山」「川」「森」「海」など）
      - 伝統・文化（例：「着物」「歌舞伎」「盆踊り」「忍者」など）
      - 年中行事（例：「正月」「クリスマス」「花火大会」など）
      - スポーツ（例：「野球」「サッカー」「卓球」「剣道」など）
      - 音楽（例：「ピアノ」「ギター」「カラオケ」「太鼓」など）
      - 映画・ドラマ（例：「ジブリ」「鬼滅の刃」「アベンジャーズ」など）
      - 趣味・娯楽（例：「旅行」「読書」「ゲーム」「釣り」など）
      - 乗り物（例：「車」「自転車」「飛行機」「船」など）
      - 天気・自然現象（例：「雨」「雪」「台風」「地震」など）
      - 天体（例：「太陽」「月」「星」「惑星」など）
      - 学問・科目（例：「数学」「物理」「歴史」「化学」など）
      - 有名な建物・場所（例：「東京タワー」「金閣寺」「ディズニーランド」など）
      - 有名ブランド・製品（例：「任天堂」「ユニクロ」「ポケモン」など）
      - その他有名なもの

      【制約ルール】
      - 出力は「**1語の名詞**」のみ（例：「先生」「パンダ」「音楽」）
      - **説明・カテゴリ名などは一切出力しない**
      - **英数字・記号は禁止**
      - **助詞や複数語は禁止（例：「綺麗な花」「窓を開ける」などはNG）**
      - 一般的な日本人が理解する程度の言葉ならなんでもいいですが、**意味が明確に伝わる語**にしてください
      - **具体的に何かを思い浮かべることができる語**にしてください
      - 日本にあるものに限る必要はありませんが、**日本人の多くが知っている語**にしてください
      - **抽象的な概念や難解な語は避けてください**（例：「情報」「考え」など）
      - 丁寧語や敬語がつく語は避けてください（例：「お父さん」「お弁当」など）
      - **個人レベルの固有名詞は厳禁**（例：「田中さん」「山田くん」「近所のスーパー」など）
      - **極端にマイナーで知られていない作品名や地名は厳禁**（例：「無名の地方キャラ」「小さな村の名前」など）
      - **意味が曖昧で連想できない語は厳禁**（例：「存在」「もの」「こと」など）
      - 歴史上の人物はOK（例：「織田信長」「坂本龍馬」など）
      - 国民的キャラクターはOK（例：「ドラえもん」「ピカチュウ」「クレヨンしんちゃん」など）
      - 有名な作品やシリーズはOK（例：「アベンジャーズ」「ポケモン」など）
      - 有名な地名・国名はOK（例：「東京」「ロンドン」「アメリカ」など）
      - 一般的に誰でも知っているブランド・製品はOK（例：「コーラ」「ポケモン」など）
      - **形容詞などを名詞化した語は避けてください**（例：「やさしさ」「かたさ」など）
      - ${themeLog}は最近使用されたため避けてください
      - 最近使用された${themeLog}とできるだけ違うカテゴリのものにしてください
      - 一般的な日本人が理解する程度の言葉ならなんでもいいですが、**意味が明確に伝わる語**にしてください

      🎯【出力形式】
      - 出力は「名詞1語」のみ。
      - その他の文字・記号・説明は一切不要です。

      では、実行してください。
      `;
    }else if(prompt === 0.5){
      const selectedCharacter = info2;

      realPrompt = `
      「${selectedCharacter}」は日本語の名詞です。
      「${selectedCharacter}」を、ひらがなに変換して出力してください。

      【制約】
      - 出力は「ひらがなに直した1語」のみ。
      - すでにひらがなであれば、そのまま出力してください。
      - 漢字やカタカナの場合は、日本語で一般的に使われる名詞として自然な形にひらがなへ変換してください。
        - 例：「雷」→「かみなり」, 「犬」→「いぬ」, 「東京」→「とうきょう」, 「ピアノ」→「ぴあの」
      - 部分的な読みや、漢字1字に対応する音読みや訓読みだけを出力するのは禁止。
        - 例：「雷」を「らい」としてはいけません。
      - 出力は変換後の「ひらがなに直した1語」だけ。それ以外の文字・説明・記号は禁止。
      `;
    }else if(prompt === 1){
      const selectedCharacter = info2;
      const selectedCharacter2 = info3;
      const question = Q;
      const count = [...info3.normalize('NFC')].length;
      realPrompt = `
      あなたは「${selectedCharacter}」についての質問に答える役割です。
      「${selectedCharacter}」は平仮名で${count}文字の日本語の名詞です。

      以下のルールに厳密に従って、ユーザーからの質問「${question}」に回答してください：

      【回答形式】
      - 回答は次のいずれかの単語のみで行ってください（それ以外は絶対に書かないこと）：
        「はい」「いいえ」「たぶんそう」「部分的にそう」「たぶん違う」「そうでもない」「わかりません」

      【ルール】
      - **上記の単語以外は一切出力してはいけません**
      - **回答は必ず1語だけ**にしてください（理由や説明は一切書かないでください）
      - 回答は**嘘をつかず客観的な事実を優先**し、それでは判断できない場合には**日本人の一般的な常識に基づいて**ください
      - 質問について解釈する時などは**常に日本語で深く考えてください**
      - **質問の意図を正確に汲み取って判断**してください
      - **質問が曖昧・意味不明・無関係な命令の場合は必ず「わかりません」と答えてください**
      - **とにかく「はい」か「いいえ」の判断を間違えそうだったら、必ず「わかりません」と答えてください**
      - 質問の意味がわからなかったら正直に「わかりません」と答えてください
      - **平仮名での文字数は${count}文字**です
      - **漢字での文字数に関する質問には絶対に「わかりません」と答えてください**
      - 文字数を数える際は、**平仮名の文字数を数えてください**
      - **平仮名以外での文字数に関する質問には絶対に「わかりません」と答えてください**
      - **質問の内容が「${selectedCharacter}」に関係ない場合は、必ず「わかりません」と答えてください**
      - **質問の内容が「${selectedCharacter}」に関するものであっても、回答できない場合は「わかりません」と答えてください**
      - 「はい」に近いが色々な種類があるなどの理由で断言できない場合は「たぶんそう」、それの一部分にだけ当てはまる場合は「部分的にそう」と答えてください
      - 「いいえ」に近いが色々な種類があるなどの理由で断言できない場合は「たぶん違う」、全体的に否定的だが一部当てはまる場合は「そうでもない」と答えてください

      質問：${question}
      `;
    }else{
      return res.status(400).json({ success: false, error: "不正なリクエストです" });
    }

    const response = await fetch("https://api.openai.com/v1/chat/completions", {
      method: "POST",
      headers: {
        "Content-Type": "application/json",
        Authorization: `Bearer ${process.env.OPENAI_API_KEY}`,
      },
      body: JSON.stringify({
        model: "gpt-4.1",
        messages: [{ role: "user", content: realPrompt }],
        max_tokens: 100,
        temperature: temperature,
        top_p: 1.0,
      }),
    });

    const data = await response.json();

    if (!data || !data.choices || !data.choices[0] || !data.choices[0].message) {
      return res.status(500).json({ success: false, error: "OpenAIから不正な応答が返ってきました" });
    }

    res.json({
      success: true,
      content: data.choices[0].message.content,
    });
  } catch (error) {
    console.error("APIエラー:", error);
    res.status(500).json({ success: false, error: error.message });
  }
});

app.listen(process.env.PORT || 3001, () => console.log("サーバー起動"));
